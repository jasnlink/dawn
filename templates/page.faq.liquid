{% assign content = page.content | replace: '<h3>', '<h3 class="mt-8 text-2xl font-semibold">' | replace: '<p>', '<p class="mt-4">' %}
<div class="container mx-auto pt-12 pb-16">
    <div class="w-full lg:w-3/5 mx-auto">
        <div class="text-4xl font-bold">{{ page.title }}</div>
        <div class="mt-8 text-lg">{{ content }}</div>
    </div>
</div>